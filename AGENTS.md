This is a modern React project. Don't import React from 'react'.
Prefer named imports and exports.

We don't use file extensions in imports. Example: `import { Thing } from './Thing'` where the filename is Thing.tsx.